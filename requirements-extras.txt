# Dépendances optionnelles pour les nouveaux tools Orion.
# Installe avec : pip install -r requirements-extras.txt
#
# Tools concernés (le serveur fonctionne sans, ces tools retournent une erreur
# explicite si la lib manque) :
#   notify          → winotify (Windows) — sur Linux/macOS, utilise notify-send/osascript
#   screenshot      → mss + pillow (cross-platform)
#   read_pdf        → pypdf
#   read_docx       → python-docx
#   mouse_*, keyboard_* → pyautogui
#   clipboard_get/set   → pyperclip
#   list_windows, focus_window, window_control → pygetwindow
#
# Note Windows : pyautogui demande pillow, déjà inclus via mss.
winotify>=1.1.0; sys_platform == "win32"
mss>=9.0.0
pillow>=10.0.0
pypdf>=4.0.0
python-docx>=1.0.0
pyautogui>=0.9.54
pyperclip>=1.8.2
pygetwindow>=0.0.9
einops>=0.8.1
huggingface_hub>=0.33.1
safetensors>=0.6.2

